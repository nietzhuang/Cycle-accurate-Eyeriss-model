filter5x5x1_ifmap81x81x1_stride2			// Verify stride larger than 1, folding larger than 1, and multiple processing passes.
filter5x5x3_ifmap81x81x3_stride2			// Verify stride larger than 1, folding larger than 1, multiple channels, and multiple processing passes.
filter11x11x3_ifmap227x227x1_stride4			// Verify very large layer. //!! Substitute it with AlexNet 1st layer later on.